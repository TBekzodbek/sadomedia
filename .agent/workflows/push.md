---
description: Automatically push all changes to GitHub
---

Push the current changes to the default remote branch.

// turbo
1. Run `npm run push` to add, commit, and push all current changes to the main branch.
